## Context

<!-- Why this change, in two or three lines. -->

## Changes

<!-- One bullet per file or area, bold path first. -->

## Notes

<!-- Trade-offs, follow-ups, anything a reviewer should know. Delete if empty. -->

## Checklist

- [ ] `./gradlew build -Proborazzi.test.verify=true` passes locally, or the change is docs-only
- [ ] Docs updated where they belong: the README only when the landing page changes (pitch, what the
      repo demonstrates, stack, quick start, docs index, roadmap "Now"); everything else in `docs/` or
      next to the code
- [ ] No hand-written number that describes the present without a source (badge, link or file)
