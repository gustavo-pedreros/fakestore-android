# Design canvas

The original canvas, kept as drawn: in Spanish, before the code. `fakestore-design-system.html` is the
published page, editor and boards in one file; the `.dc.html` files and `canvas.json` are its sources.
What shipped, and the names that drifted, are in the [design system doc](../../design-system.md#boards).
